#include <stdio.h>
/**
 * mul - Entry point
 *
 * @i: Enter  number for var i to compute
 *
 * @j: Enter numbe for var k to compute
 *
 * Return: Always 0 (Success)
 */
int mul(int i, int j)
{
	return (i * j);
}
